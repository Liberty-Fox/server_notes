name: server_notes
description: Notes cloud app.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^6.1.1
  cloud_firestore: ^0.16.0
  cupertino_icons: ^1.0.2
  firebase_core: ^0.7.0
  flutter_bloc: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true